# Requirements for Direction Module on Raspberry Pi 4 Model B
# Install with: pip3 install -r requirements.txt

# Core dependencies
opencv-python>=4.5.0
numpy>=1.19.0

# Optional: OpenCV headless version (if you don't need GUI features)
# opencv-python-headless>=4.5.0

# Note: Some system dependencies still need to be installed via apt:
# sudo apt install -y build-essential cmake pkg-config git python3 python3-pip python3-dev
# sudo apt install -y libjpeg-dev libtiff5-dev libpng-dev libavcodec-dev libavformat-dev libswscale-dev libv4l-dev libxvidcore-dev libx264-dev libgtk-3-dev libcanberra-gtk3-dev libatlas-base-dev gfortran
